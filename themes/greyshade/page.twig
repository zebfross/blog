{% extends "_layouts/default.twig" %}
{% block content %}
<article class="page">
    {% if page.title %}
    <h1 class="title">{{ page.title }}</h1>
    {% endif %}
    <div class="entry-content">{{ content }}</div>
</article>
{% endblock %}
